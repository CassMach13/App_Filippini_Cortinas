{
  "name": "filippini-cortinas",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:syntax": "node --check apps.js && node --check order-domain.js"
  },
  "devDependencies": {
    "firebase-tools": "15.30.0"
  }
}
